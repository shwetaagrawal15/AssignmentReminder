<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@drawable/list"
    android:clickable="false"

    tools:context="com.contactmanager.home.assignmentreminder.MainActivity"
    style="@style/TextAppearance.Widget.AppCompat.Toolbar.Title"
    android:textAlignment="center"
    android:id="@+id/Text">



        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ENTER"
            android:id="@+id/add"
            android:outlineProvider="background"
            android:background="#ff23acff"
            android:onClick="onClickAddAssignment"
            android:layout_alignTop="@+id/editText"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:layout_marginRight="191dp"
            android:layout_marginEnd="191dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/editText"
        android:layout_centerVertical="true"
        android:layout_alignRight="@+id/add"
        android:layout_alignEnd="@+id/add" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="Assignment Reminder"
        android:id="@+id/textView"
        android:layout_above="@+id/add"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:capitalize="none"
        android:textIsSelectable="false"
        android:textAlignment="center"
        android:textColorHint="#ffff1ec3"
        android:textColor="#ff2441ff" />
</RelativeLayout>

